## UserRoles (v1)

Базовый CRUD для справочника ролей пользователей. Это “эталон” для остальных CRUD‑контроллеров.

Базовый путь: `/api/v1/user-roles`

### Модель

`UserRoleDto`:

```json
{
  "id": "00000000-0000-0000-0000-000000000000",
  "name": "Student",
  "displayName": "Студент",
  "createdAt": "2026-02-03T12:34:56.789Z",
  "updatedAt": null
}
```

### GET `/api/v1/user-roles`

Список (пагинация + поиск).

**Query params**:
- `q` (string, optional): поиск по `Name`/`DisplayName`
- `page` (int, optional, default=1)
- `pageSize` (int, optional, default=50, 1..200)

**Ответ 200**:

```json
{
  "page": 1,
  "pageSize": 50,
  "total": 4,
  "items": []
}
```

### GET `/api/v1/user-roles/{id}`

Получить роль по `id`.

- **Ответ 200**: `UserRoleDto`
- **Ответ 404**: роль не найдена

### POST `/api/v1/user-roles`

Создать роль.

**Body**:

```json
{
  "name": "NewRole",
  "displayName": "Новая роль"
}
```

- **Ответ 201**: `UserRoleDto`
- **Ответ 400**: ошибка валидации
- **Ответ 409**: конфликт `name` (уже существует)

### PUT `/api/v1/user-roles/{id}`

Обновить роль.

**Body**:

```json
{
  "name": "NewRole",
  "displayName": "Обновлённая роль"
}
```

- **Ответ 200**: `UserRoleDto`
- **Ответ 400**: ошибка валидации
- **Ответ 404**: роль не найдена
- **Ответ 409**: конфликт `name`

### DELETE `/api/v1/user-roles/{id}`

Удалить роль.

- **Ответ 204**: удалено
- **Ответ 404**: роль не найдена

